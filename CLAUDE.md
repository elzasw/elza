# ELZA

The main project lives in the `elza/` directory. See `elza/CLAUDE.md` for full project documentation.

The `integration/` directory is obsolete and will be removed. The `elza/` directory will become the repository root.

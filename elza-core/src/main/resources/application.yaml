application:
  version: ${project.parent.version}

spring:
  datasource:
    url: jdbc:postgresql://vyv-inet:5432/elza_vanek
#    url: jdbc:postgresql://vyv-inet/elza_test    
#    url: jdbc:postgresql://vyv-inet/elza_test_data
    username: app
    password: app
  data:
    rest:
      base-uri: /rest
  jpa:
    # Logovani SQL dotazu
    show-sql: true
vax:
  viewMain: FindingAidList
  viewError: Exception

debug:
  firstView: FindingAidList

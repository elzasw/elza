/**
 * Node id and version
 *
 * Object is used to transfer nodeId and its version
 */
export interface ArrNodeVO {
    /**
     * identifikátor uzlu
     */
    id: number;

    /**
     * verze uzlu
     */
    version: number;
}
